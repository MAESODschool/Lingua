# รายงานตรวจระบบเกม Lingua

วันที่ 9 กันยายน 2026 · โค้ดฐาน commit `703b553`

ตรวจโค้ดและการตั้งค่าในเครื่อง รันชุดทดสอบ ตรวจข้อมูลคำถาม และทดลองหน้าเว็บบางส่วนผ่าน `http://127.0.0.1:8787` รายงานนี้ไม่ใช่การรับรองว่าทุกเส้นทางในเกมหรือระบบที่เผยแพร่จริงผ่านแล้ว ไม่มีการแก้โค้ดเกมหรือ deploy กฎฐานข้อมูลในการตรวจครั้งนี้

## ผลโดยรวม

ระบบมีความพร้อมสำหรับการทดสอบแบบควบคุมมากขึ้นจากรายงานวันที่ 30 สิงหาคม แต่ยังควรแก้การแทรก HTML ผ่านชื่อผู้เล่นและสิทธิ์แก้สถานะ PvP ก่อนเปิดใช้งานวงกว้าง หากเปิด AI backend ต้องเพิ่มการยืนยันผู้เรียกและควบคุมโควตา ส่วนมือถือยังมีภาระไฟล์ภาพเคลื่อนไหวขนาดใหญ่

- ชุดทดสอบเดิมผ่าน **8/8 ชุด**
- คลังคำถาม Act 1 ผ่านตัวตรวจที่มีอยู่ **13 คลัง × 100 ข้อ = 1,300 ข้อ**
- ตัวตรวจลำดับด่านผ่าน ไม่พบ stage ID ที่ขาดจากลำดับที่กำหนด
- โครงสร้าง Act มี 17 stage entries รวมฉากเนื้อเรื่องและฉากจบ; รายการฝึกบอสมี 11 บอส
- คลังฝึกกริยา 3 ช่องมี 169 รายการ แต่ครั้งนี้ยังไม่ได้ตรวจคำแปลและความถูกต้องทางภาษาทีละรายการ
- ตรวจไวยากรณ์ JavaScript ของเกม ข้อมูลกริยา และ backend ผ่าน
- HTML มี 372 IDs ไม่ซ้ำ ไม่พบไฟล์หายจากการตรวจเส้นทาง `assets/...` ที่เป็นข้อความคงที่

## ประเด็นที่ต้องแก้

### 1. สูง — ชื่อผู้เล่นเข้าสู่ HTML ของผลประเมินโดยไม่ escape

**หลักฐาน:** `script.js:30665` ส่ง `characterName`/`displayName` เป็น `evaluation.playerName`; `script.js:30689`–30690 นำค่าไปแทรกใน `panel.innerHTML` โดยตรง ขณะที่เส้นทางตั้งชื่อ `confirmStoryName` ที่ `script.js:29644` ตรวจเพียงชื่อไม่ว่างแล้วบันทึก

ชื่อที่มี markup จึงถูกตีความเป็น HTML ในหน้าผลประเมิน เป็นช่องทางเสี่ยง stored XSS ภายใน session ที่แสดงชื่อนั้น การตรวจครั้งนี้ยืนยันเส้นทางข้อมูลจากโค้ด ไม่ได้ใส่ payload อันตรายในข้อมูลผู้เล่น และยังไม่ได้พิสูจน์การโจมตีข้ามบัญชี

**แก้ไข:** สร้างส่วนแสดงชื่อด้วย `textContent` หรือใช้ `escapeHtml` ที่มีอยู่แล้ว เพิ่มกรณีทดสอบชื่อที่มี `<`, `>`, เครื่องหมายคำพูด และข้อความที่ดูเหมือน tag

### 2. สูง — ผู้เข้าร่วม PvP แก้ข้อมูลการต่อสู้ได้เกินขอบเขต

**หลักฐาน:** `firestore.rules:377`–379 อนุญาต update เมื่อเป็น participant และยังคง room identity; `keepsPvpRoomIdentity` ตรวจเพียงรหัสห้องและ UID หลัก ไม่จำกัด HP, players, lanes, ผลชนะ หรือการเปลี่ยนสถานะ ส่วน `script.js:33674` ใช้ transaction เขียน room object จากฝั่งผู้เล่น

แม้แก้ปัญหาคนนอกอ่าน/แก้ทุกห้องจากรายงานเดิมแล้ว ผู้เล่นในห้องยังสามารถส่งข้อมูลที่เปลี่ยนสถานะฝ่ายตรงข้ามหรือผลการแข่งขันได้ตามเงื่อนไขกฎปัจจุบัน จึงยังใช้เป็นผลการแข่งขันที่เชื่อถือไม่ได้

**แก้ไข:** ตรวจคำตอบและคำนวณผลสำคัญบน backend; จำกัด field และ state transition ตามบทบาท เพิ่ม Firestore Emulator tests สำหรับการแก้ HP ฝ่ายตรงข้าม ปลอมผลชนะ และแก้ห้องหลังจบ การตรวจครั้งนี้อ่านกฎใน repo ไม่ได้โจมตีฐานข้อมูลจริงหรือยืนยันกฎที่ deploy

### 3. สูงเมื่อเปิด AI — Advisor เรียกได้โดยไม่ยืนยันบัญชี

**หลักฐาน:** `api/lingua-advisor.js` ยอมรับคำขอไม่มี Origin และ shared secret เป็นทางเลือก เอกสารแนะนำปล่อย secret ว่างเมื่อใช้ browser โดยตรง ไม่มีการตรวจ Firebase ID token ใน handler

ทดสอบ handler แบบจำลอง upstream แล้วได้ **HTTP 200 และเรียก upstream 1 ครั้ง** สำหรับคำขอไม่มี Origin และไม่มีข้อมูลยืนยันตัวตน แม้ตั้ง allowed origin ไว้แล้ว CORS จึงไม่ได้จำกัดโปรแกรมที่เรียก endpoint โดยตรง

Rate limit เก็บใน Map ของ process (`api/lingua-advisor.js:90`) ไม่แชร์ข้าม serverless instances และไม่ได้ลบ bucket ของ IP เก่าที่ยุติใช้งาน จึงยังไม่เพียงพอสำหรับคุมค่าใช้จ่ายระดับบริการ การเชื่อถือ `x-forwarded-for` ต้องประเมินร่วมกับ host ที่ใช้จริง

**แก้ไข:** ตรวจ token ฝั่ง server และใช้โควตาที่เก็บส่วนกลางพร้อมขีดจำกัดค่าใช้จ่าย ก่อนเปิด endpoint สาธารณะ ปัจจุบัน `index.html:7` ยังตั้ง API URL ว่าง จึงเป็นข้อเสี่ยงของการเปิด backend ไม่ใช่หลักฐานว่ากำลังมีค่าใช้จ่ายหรือถูกใช้งานผิดอยู่

### 4. สูงด้านประสิทธิภาพ — ไฟล์ภาพเคลื่อนไหวใหญ่และโหลดล่วงหน้าหลายรายการ

โฟลเดอร์ assets มี 95 ไฟล์ รวม **1,048,579,466 bytes (~1.05 GB)** ตัวเลขนี้คือขนาดไฟล์ในโครงการ ไม่ใช่ยอดดาวน์โหลดหน้าแรก เพราะมีทั้งต้นฉบับและไฟล์ที่ไม่ได้ใช้

ไฟล์ที่มีการอ้างอิงใน runtime ได้แก่ `assets/characters/yesterday-spirit-transparent.gif` ประมาณ 42.55 MB และ `assets/backgrounds/grammar-hall-animated.gif` ประมาณ 42.14 MB นอกจากนี้ยังมีไฟล์ต้นฉบับ GIF ขนาด 60–89 MB หลายไฟล์

`script.js:22602` preload ฉาก Act 1 และ fallback ทั้งชุด; `script.js:24180` สร้างทุก BGM track และตั้ง `preload = auto` การดาวน์โหลดจริงขึ้นกับ browser แต่โครงสร้างนี้เพิ่มความเสี่ยงโหลดช้า ใช้หน่วยความจำสูง และสะดุดบนมือถือ

**แก้ไข:** ลดขนาด animation และ resolution ตามจอ โหลดภาพ/เพลงเมื่อจะใช้ แยกไฟล์ต้นฉบับออกจากชุดเผยแพร่ แล้ววัด cold load บนมือถือและเครือข่ายจำกัดก่อนกำหนดงบขนาดไฟล์

### 5. กลาง — หน้าต่างบทเรียนไม่จัดการโฟกัสคีย์บอร์ด

**ยืนยันใน browser:** เปิดแผนที่บทเรียนแล้วโฟกัสยังอยู่ปุ่ม `lessonMapButton` เมื่อกด Tab โฟกัสไป `vsBossesButton` ที่อยู่หลัง modal แทนที่จะเข้าเนื้อหาหน้าต่าง

`openGameModal` ที่ `script.js:25440` แสดง modal แต่ไม่มี focus trap หรือย้ายโฟกัส ส่วน HTML มี `aria-modal=true` อยู่แล้ว การใช้คีย์บอร์ดและโปรแกรมอ่านหน้าจอจึงไม่สอดคล้องกับการแสดงผล

**แก้ไข:** ย้ายโฟกัสเข้าหน้าต่างเมื่อเปิด จำกัด Tab ภายใน ทำพื้นหลัง inert และคืนโฟกัสให้ปุ่มเดิมเมื่อปิด

### 6. กลาง — ยังไม่มีหลักฐานทดสอบระบบออนไลน์ครบวงจร

ชุดทดสอบปัจจุบันใช้การอ่าน source และแยกฟังก์ชันมารันใน VM พร้อม mock จึงช่วยจับ regression เฉพาะจุดได้ แต่ไม่ยืนยัน Firebase Auth จริง, Firestore rule evaluation, การแข่งพร้อมกันสองคน, session ข้ามเครื่อง หรือปัญหา network race

ไม่มี `package.json` หรือชุดตั้งค่า runner/emulator ในโครงการที่ตรวจพบ ไฟล์หลัก `script.js` มี 50,791 บรรทัด (~1.94 MB) และ CSS 11,487 บรรทัด (~246 KB) ทำให้เปลี่ยนระบบหนึ่งแล้วกระทบอีกระบบได้ง่าย

**แก้ไข:** เพิ่มชุดทดสอบ emulator และ browser journeys ที่ใช้บัญชีทดสอบ แยกโมดูล auth/save, battle, question data, PvP และ teacher dashboard ทีละส่วนโดยคงผลทดสอบเดิม

## สถานะแต่ละระบบและหลักฐาน

| ระบบ | ผลตรวจครั้งนี้ | ขอบเขตที่ยังไม่ยืนยัน |
|---|---|---|
| เข้าเกม/บัญชี | browser คืน session เดิมและเปิดเมนูได้; online-login-safety ผ่าน 32 checks | สมัครใหม่ รหัสผ่านผิด token หมดอายุ และ offline จริง |
| ตัวละคร/โปรไฟล์ | ตรวจเส้นทางบันทึกและข้อจำกัด field; พบจุดแสดงชื่อไม่ปลอดภัย | สร้างตัวละครใหม่ครบ flow |
| บทเรียน/เนื้อเรื่อง | เปิดแผนที่ได้; progression validator ผ่าน; CVC flow ผ่าน regression | เล่นครบตั้งแต่เริ่มจนฉากจบ |
| คำถาม Act 1 | ทุกคลังผ่าน validator: จำนวน ID คำถามซ้ำในคลัง เฉลยและข้อมูลหลักตามชนิดที่ validator รองรับ | ความถูกต้องภาษาและคุณภาพการสอนทีละข้อ รวมถึงโจทย์นอก Act 1 |
| PvE | regression ผ่าน damage, charms, ตอบผิด, double-click, Direct Attack, cooldown, handoff ไปชัยชนะ | สมดุลเกม การต่อสู้ทุกบอสจริง เสียง/animation ทุกจังหวะ |
| VS Bosses | แสดง 11 บอสใน browser; rank/history และ Rewind Slime/CVC regression ผ่าน | เล่นฝึกจนจบและบันทึกคะแนนทุกบอส |
| ฝึกกริยา 3 ช่อง | อ่านข้อมูลได้ 169 รายการ | การสุ่มครบทุกช่องและ UX การตอบจริง |
| เซฟ/ความคืบหน้า | player-write-security ผ่าน 24 checks; แยก `playerClientProgress` จาก field ทางการ | เซฟ/โหลดสองเครื่องและ conflict ขณะ offline |
| Teacher Dashboard | tests permission ผ่าน 17 checks และ no-Storage ผ่าน 21 assertions; rules จำกัด teacher claims | สิทธิ์บัญชีจริง การแก้/คืนข้อมูลในฐานจริง |
| PvP | ตรวจพบ join/update/combat implementation แล้ว; สิทธิ์คนนอกจำกัดขึ้น | การเล่นสอง client และความเชื่อถือผลการแข่งขัน |
| Advisor | method/origin/payload guard และการตัดเฉลยก่อนตอบผ่านทดสอบจำลอง | คุณภาพคำตอบจาก AI จริงและการ deploy backend |
| Asset Manager | ตั้ง `ASSET_MANAGER_ENABLED=false`; upload safety ผ่าน 71 checks และ boss assets ผ่าน | การเปิดใช้งาน/อัปโหลดจริง |
| เพลง | lesson-music-routing ผ่าน 27 checks | ฟังเพลงและตรวจ autoplay บน iOS/Android จริง |
| UI | เมนู แผนที่ VS Bosses เปิดได้; ที่ 390×844 ไม่มี horizontal overflow ในเมนู | จอ battle/teacher ทุก breakpoint; browser อื่น |

คะแนนจาก `playerClientProgress` เป็นข้อมูลที่ browser รายงานเองตามการออกแบบ ควรใช้ติดตามการฝึกพร้อมระบุแหล่งข้อมูล และแยกจากคะแนนทางการที่ผ่านการตรวจฝั่ง server

## เปรียบเทียบรายงานเดิม

- รายงานเดิมว่าผู้ใช้ทั่วไป list ผู้เรียนทั้งหมดได้: **กฎใน repo แก้แล้ว** โดยจำกัด list ให้ teacher
- รายงานเดิมว่ากรอก PIN อย่างเดียวได้สิทธิ์ครู: **มี custom claim เป็นขอบเขตสิทธิ์จริงแล้ว** PIN เป็นเพียง UI gate
- รายงานเดิมว่าข้ามบทเรียนหลังชัยชนะ: **มีลำดับด่านกลางและตัวตรวจผ่านแล้ว** ยังไม่ได้เล่นจบทุกด่านด้วย browser เพื่อยืนยันทั้งหมด
- รายงานเดิมว่าโจทย์ว่างนับผ่านด่าน: **พบ flow แสดง content error และคืนเมนูแล้ว** ไม่ควรยกข้อความเก่ามาเป็นข้อผิดพลาดปัจจุบันโดยอัตโนมัติ
- รายงานเดิมว่า online PvP ไม่มี combat: **พบฟังก์ชันส่งโจทย์ ตอบ ใช้สกิล และ resolve action แล้ว** แต่ยังไม่ทดสอบสองบัญชีจริง
- คำถามซ้ำในคลัง Act 1 ที่ validator ตรวจ: **รอบนี้ไม่พบ**
- debug flags: **ปิดแล้ว**; ยังมี content validator logs บางส่วน
- ชื่อผู้เล่นในผลประเมิน, ขนาด assets, modal focus และโครงสร้างไฟล์ใหญ่: **ยังพบอยู่**

## วิธีตรวจและไฟล์หลักฐาน

1. ตรวจ source, HTML/CSS, `firestore.rules`, `storage.rules`, `firebase.json` และเอกสาร Advisor
2. รัน `node --check` กับไฟล์ JavaScript หลัก 3 ไฟล์
3. รัน `node` กับไฟล์ `tests/*.cjs` ทั้ง 8 ไฟล์ ผลเก็บที่ `work/audit-2026-09-09/regression-results.json`
4. โหลดส่วนข้อมูลจาก source ปัจจุบันใน Node VM และเรียกตัวตรวจคลังคำถาม/ลำดับด่าน ผลเก็บที่ `work/audit-2026-09-09/question-validation.json`
5. ทดสอบ Advisor handler โดยแทน upstream ด้วย mock ไม่มีการเรียก AI หรือใช้ API key จริง ผลเก็บที่ `work/audit-2026-09-09/advisor-validation.json`
6. เปิด browser ด้วย session ทดสอบที่มีอยู่ ตรวจเมนู แผนที่ Tab focus รายการบอส และขนาดเมนูมือถือ ไม่สร้างบัญชี ไม่เริ่มการแข่งขัน ไม่อัปโหลดหรือแก้คะแนน

ไม่มี error/warn ใน console ที่อ่านในช่วงเปิดเมนูและแผนที่ แต่ไม่ได้ใช้ผลนี้รับรองว่าทั้งเกมไม่มี runtime error การเปิดหน้าต่างและเปลี่ยนฉากอาจบันทึก last-scene ตามกลไกเดิมของเกม

ไม่ได้ตรวจ production deployment, custom claims ที่ออกให้บัญชีจริง, การกู้คืนฐานข้อมูล, ความเข้ากันได้ทุกอุปกรณ์, load test, การเล่นพร้อมกันจำนวนมาก หรือการเข้าถึงทุกหน้าด้วย screen reader

## ลำดับดำเนินการที่แนะนำ

1. แก้ชื่อผู้เล่นในผลประเมิน และเพิ่ม regression ป้องกัน HTML injection
2. ทำผล PvP ให้ตรวจสอบฝั่ง server และเพิ่ม emulator tests
3. เพิ่ม auth/quota ของ Advisor ก่อนเปิด endpoint
4. ลดไฟล์ animation และปรับโหลดตามใช้งาน พร้อมวัดมือถือจริง
5. แก้ modal focus และทดสอบคีย์บอร์ด
6. ทำ end-to-end test ตั้งแต่สมัคร → เรียน → ต่อสู้ → เซฟ → กลับเข้าเกม และทดสอบ PvP สองบัญชี

รายงานนี้ส่งมอบผลตรวจและสิ่งที่ยังต้องพิสูจน์ ยังไม่ได้ดำเนินการแก้ข้อผิดพลาดข้างต้น
